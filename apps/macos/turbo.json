{
  "extends": ["//"],
  "tasks": {
    "build": {
      "inputs": ["$TURBO_DEFAULT$", "$TURBO_ROOT$/packages/core/Sources/**"],
      "outputs": ["build/Afterhours.app/**"],
      "env": ["ARCHS", "SDKROOT"]
    }
  }
}
